# backend/ — INERTE (no se usa)

Este directorio contiene el **boilerplate generado por Emergent** (FastAPI + MongoDB:
un simple endpoint `status_check` de ejemplo en `server.py`). **No forma parte del
producto KitchenBase.**

La aplicación habla **directamente con Supabase** desde el frontend; no hay servidor
propio en el bucle. Este código se conserva solo como referencia histórica del
andamiaje de Emergent y podría eliminarse en el futuro.

⚠️ **No construir nada nuevo aquí.** Ver la sección *Arquitectura* en `CLAUDE.md` y la
entrada correspondiente en la deuda técnica (`Documentación/`).
